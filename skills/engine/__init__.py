"""Compatibility import surface for the canonical :mod:`airp.engine` package.

Production implementations live in ``src/airp/engine``. These modules remain
under ``skills`` only so legacy scripts and user extensions keep importing
``engine.*`` during the migration.
"""
